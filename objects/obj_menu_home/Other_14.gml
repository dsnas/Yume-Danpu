/// @descr Option move result
// Refer to {lvl[lvl_curr].option_curr} as {_opt_curr}
// Disable the movement sound by modifying the variable {_move_snd} to -1

if (lvl_curr == LVL_LANG)
	fn_config_lang_mod(_opt_curr);
